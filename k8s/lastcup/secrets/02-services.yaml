apiVersion: v1
kind: Service
metadata:
  name: lastcup-api-active
  namespace: lastcup
spec:
  type: ClusterIP
  selector:
    app: lastcup-api
  ports:
    - name: http
      port: 80
      targetPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: lastcup-api-preview
  namespace: lastcup
spec:
  type: ClusterIP
  selector:
    app: lastcup-api
  ports:
    - name: http
      port: 80
      targetPort: 8080
